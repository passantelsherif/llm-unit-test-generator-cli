# llm-unit-test-generator-cli
A deterministic CLI-based LLM developer tool that generates strict, runnable Python unit tests for a single function with enforced output constraints and security safeguards.
